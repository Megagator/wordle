//
//  Style.swift
//  Grands Prix
//
//  Created by Mike Reinhard on 2/21/21.
//

import Foundation

import SwiftUI
import UIKit

extension Color {
    // System
    static let lightText = Color(UIColor.lightText)
    static let darkText = Color(UIColor.darkText)

    static let label = Color(UIColor.label)
    static let secondaryLabel = Color(UIColor.secondaryLabel)
    static let tertiaryLabel = Color(UIColor.tertiaryLabel)
    static let quaternaryLabel = Color(UIColor.quaternaryLabel)

    static let systemBackground = Color(UIColor.systemBackground)
    static let secondarySystemBackground = Color(UIColor.secondarySystemBackground)
    static let tertiarySystemBackground = Color(UIColor.tertiarySystemBackground)
    
    static let whiteText = Color("WhiteText")
    static let border = Color("Border")
    static let keyCap = Color("KeyCap")
    static let keyText = Color("KeyText")
    
    static let correct = Color("Green")
    static let wrongPlacement = Color("Yellow")
    static let absent = Color("Absent")
}
